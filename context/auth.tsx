"use client"

// Re-export everything from the auth context
export { AuthProvider, useAuth } from "@/components/auth/auth-context"
export type { User, AuthContextType } from "@/components/auth/auth-context"
